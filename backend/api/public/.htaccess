<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all requests to Symfony front controller when no real file/dir exists.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>

